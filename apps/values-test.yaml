argoApps:
  applications:
    oth-sync-apps:
      path: apps
      targetRevision: main
      valueFiles:
        - values.yaml
        - values-test.yaml
      namespace: oth-sync

    test-website:
      path: test-website
      targetRevision: main
      valueFiles:
        - values.yaml
        - values-test.yaml
      namespace: oth-sync

    kih-telecate-exporter:
      path: kih-telecate-exporter
      targetRevision: main
      valueFiles:
        - values.yaml
        - values-test.yaml
