argoApps:
  applications:
    oth-sync-apps:
      path: apps
      targetRevision: main
      valueFiles:
        - values.yaml
        - values-prod.yaml
      namespace: oth-sync

    test-website:
      path: test-website
      targetRevision: main
      valueFiles:
        - values.yaml
        - values-prod.yaml
      namespace: oth-sync
